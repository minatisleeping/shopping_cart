export enum UserVerifyStatus {
  Unverified,
  Verified,
  Banned,
}

export enum USER_ROLE {
  Admin,
  Staff,
  User,
}

export enum TokenType {
  AccessToken,
  RefreshToken,
  ForgotPasswordToken,
  EmailVerificationToken,
}
